Quick hackjob verison of Sudoku with ncurses. Improvements to come!

To build, `cd src && make`, which drops a binary in src. Requires ncurses.so to
compile (i.e. libncurses5-dev in Ubuntu). If you want to use a compiler other
than clang, see the Makefile.

Alex Hirzel <alex@hirzel.us>
